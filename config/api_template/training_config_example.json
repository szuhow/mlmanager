{
    "model_name": "ARCADE Classification Model",
    "dataset_id": 1,
    "model_type": "classification",
    "architecture": "resnet50",
    "description": "Automatic coronary artery classification using ARCADE dataset",
    "is_public": false,
    "hyperparameters": {
        "learning_rate": 0.001,
        "batch_size": 32,
        "epochs": 100,
        "optimizer": "adam",
        "weight_decay": 0.0001,
        "early_stopping_patience": 10,
        "reduce_lr_patience": 5,
        "validation_split": 0.2
    }
}
